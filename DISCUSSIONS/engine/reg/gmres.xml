<!-- 
  $RCSfile: gmres.xml,v $
  $Revision: 1.3 $
  $Author: langer $
  $Date: 2011-04-25 02:42:42 $

  This software was produced by NIST, an agency of the
  U.S. government, and by statute is not subject to copyright in the
  United States.  Recipients of this software assume all
  responsibilities associated with its operation, modification and
  maintenance. However, to facilitate maintenance we ask that before
  distributing modified versions of this software, you first contact
  the authors at oof_manager@nist.gov.
-->


<para>
  The <classname>GMRES</classname> matrix method implements the
  preconditioned <ulink
  url="http://netlib2.cs.utk.edu/linalg/html_templates/node29.html"
  role="external">Generalized Minimal Residual</ulink> method for
  <link linkend="Section-Concepts-Mesh-Solvers-Matrix">solving</link>
  linear matrix equations in <xref
  linkend="RegisteredClass-AdvancedSolverMode"/>.  The matrix does not
  have to be symmetric.
</para>
<para>
  <classname>GMRES</classname> is an iterative method, meaning that it
  creates an approximate solution and improves it on each iteration.
  The iteration will cease when the relative error of the solution is
  less than the given <varname>tolerance</varname>.  The solver will
  stop and report an error if the tolerance isn't satisfied within
  <varname>max_iterations</varname> iterations.
</para>
<para>
  <classname>GMRES</classname> (and other <ulink
  url="http://netlib2.cs.utk.edu/linalg/html_templates/node50.html"
  role="external">Krylov space methods</ulink>) work by searching for
  a solution in a subspace of the full vector space.<footnote>
  <para>
    If <inlineequation>
    <alt role="tex">\(x_0\)</alt>
  </inlineequation>
  is an
  initial guess for the solution of
  <inlineequation>
    <alt role="tex">\(Ax=b\)</alt>
    </inlineequation>,
    then the initial residual is
    <inlineequation>
      <alt role="tex">\(r=Ax_0-b\)</alt>
      </inlineequation>.
      The Krylov space of dimension
      <inlineequation>
	<alt role="tex">\(k\)</alt>
	</inlineequation> is spanned by the
	vectors
	<inlineequation>
	  <alt role="tex">\(\{r,\ Ar,\ A^2r,\dots,\ A^{k-1}r\}\)</alt>
	</inlineequation>.
	GMRES finds the best solution in this space, and then restarts
	with a new
	<inlineequation>
	  <alt role="tex">\(x_0\)</alt>
	  </inlineequation>.
	  If
	  <inlineequation>
	    <alt role="tex">\(k\)</alt></inlineequation> is large
	    enough, fewer restarts are required, but more data must be retained.
  </para>
    </footnote>
    The dimension of this subspace is given by the
    <varname>krylov_dimension</varname> parameter.  Using a larger
    <varname>krylov_dimension</varname> will improve the convergence of
    the method, but will require more memory.
</para>
<para>
  The <varname>preconditioner</varname> parameter specifies which
  <xref linkend="RegisteredClass-Preconditioner"/> to use, if any.
  <link
      linkend="RegisteredClass-Preconditioner"><classname>Preconditioners</classname></link>
  speed the convergence of iterative methods by replacing the
  original matrix by something closer to the identity matrix.
</para>


  <!-- Keep this comment at the end of the file
  Local variables:
  sgml-omittag:t
  sgml-shorttag:t 
  sgml-namecase-general:nil
  sgml-minimize-attributes:nil
  sgml-always-quote-attributes:t
  sgml-indent-step:2
  sgml-indent-data:t
  sgml-parent-document:("../../../man_oof2.xml" "book" "chapter" "refentry" "refsect1")
  sgml-exposed-tags:nil
  sgml-local-ecat-files:nil
  End:
  -->
